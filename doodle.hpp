#ifndef DOODLE_H#define DOODLE_H#include <vector>#include <string>#include <sstream>#include "shape.hpp"using namespace std;#define DOODLE_LEFT_IMG "pictures/left.png"#define DOODLE_RIGHT_IMG "pictures/right.png"#define DOODLE_LEFT_J_IMG "pictures/left_jumping.png"#define DOODLE_RIGHT_J_IMG "pictures/right_jumping.png"#define FREESANS_FONT "FreeSansBold-Rdmo.otf"#define DELAY_TIME 0.005#define	START_VELOCITY -80#define	ACCELERATION 10#define	MIDDLE_OF_PAGE 500#define WINDOW_HEIGHT 1000#define FINISH_BENDING_LEGS 700#define RIGHT 1#define LEFT -1class Doodle : public Shape {public:	Doodle(float _x, float _y, string n, float wid, float hei);	string get_final_score(){ return final_score; }	int get_when_junp(){ return when_jumping; }	float get_vy() { return vy; }	void update(vector<Shape*>& shapes, Window *window);	void show_score(Window *window, bool& finish_game);	void draw(Window *window, int check_pressed_char);	void move(float d);	void exit_the_page(Doodle doodle, Window *window);	void collision_plat();	void collision_enemy(bool& finish_game);	void collision_spring();	void change_when_jump();private:	float g;	float vy;	float dy;	float score;	float first_achieved_s;	string final_score;	int when_jumping;};#endif